apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: manager-egress
  namespace: wazuh
spec:
  podSelector:
    matchLabels:
      app: wazuh-manager
      node-type: master
  policyTypes:
  - Egress
  egress:
  - to:
    - podSelector:
        matchLabels:
          app: wazuh-indexer
    ports:
    - protocol: TCP
      port: 9200
